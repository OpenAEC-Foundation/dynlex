{
  "mcpServers": {
    "lsp": {
      "command": "bash",
      "args": [
        "-c", "mcp-language-server -workspace . -lsp \"$(ls /usr/bin/clangd-* 2>/dev/null | sort -V | tail -1)\" -- --background-index"
      ]
    }
  }
}
