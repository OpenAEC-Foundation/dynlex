import lib/std.dl

flex section around:
    replacement:
        set marker to 1
        execute the body
        set marker to 2

function observed:
    execute:
        set marker to 0
        set captured to 0
        around:
            set captured to marker
        return captured

if observed = 1:
    print 1 as line
else:
    print 2 as line
