import lib/cadkernel/arena.dl
class:
    patterns:
        [an|] arena bad clone
    members:
        number as an integer
function the cad payload clone of {an arena bad clone:original}:
    execute:
        return the number of original
set nodes to a cad arena of an arena bad clone
set duplicated to the cad clone of nodes
free cad arena nodes
free cad arena duplicated
