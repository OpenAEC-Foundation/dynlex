import lib/std.dl

function the result after stage one for value:
    execute:
        return the result after stage two for value

function the result after stage two for value:
    execute:
        return the result after stage three for value

function the result after stage three for value:
    execute:
        return value + 1

print the result after stage one for 1 as a line
