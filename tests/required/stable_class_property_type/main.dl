function set variable to value:
    replacement:
        @intrinsic("store", variable, value)

function [an|] integer:
    replacement:
        @intrinsic("type", "int")

function [a|] C string:
    replacement:
        @intrinsic("type", "string")

class:
    patterns:
        [a|] box
    members:
        item

set instance to @intrinsic("construct", a box, 1)
set instance's item to "wrong"
