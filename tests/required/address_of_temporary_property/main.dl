function ignore value:
    replacement:
        @intrinsic("discard", value)

function [an|] integer:
    replacement:
        @intrinsic("type", "int")

class:
    patterns:
        box
    members:
        value as an integer

ignore @intrinsic("address of", @intrinsic("property", @intrinsic("construct", box, 1), "value"))
