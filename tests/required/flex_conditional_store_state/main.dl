import lib/std.dl

function bounded number:
    execute:
        set result to number
        clamp result between 0 and 10
        return result

print bounded (0 - 5) as a line
print bounded 20 as a line
print bounded 5 as a line
