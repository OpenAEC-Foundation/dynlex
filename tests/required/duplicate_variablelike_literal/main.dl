import lib/std.dl

function draw a rectangle using width as the width and depth as the depth:
    execute:
        print width as a line
        print depth as a line

function [report width as|report no value for] [the width|the measurement]:
    execute:
        print width as a line

draw a rectangle using 48 as the width and 12 as the depth
report 7 as the width
