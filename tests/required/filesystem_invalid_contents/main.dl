import lib/filesystem.dl

set destination to "tests/required/filesystem_invalid_contents/output.tmp"
delete the filesystem entry at destination
set empty to 0 as a pointer to a byte
set contents to a new string from empty containing -1 bytes
write contents to the file at destination and print whether it succeeded as a line
print whether destination is readable as a line
