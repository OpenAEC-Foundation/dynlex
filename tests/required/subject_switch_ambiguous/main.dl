import lib/std.dl

function publish choice for selection:
    execute:
        match on selection:
            case 1:
                set the subject to 10
            otherwise:
                set the subject to 20
        print it as a line

publish choice for 1
