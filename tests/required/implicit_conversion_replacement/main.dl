import lib/std.dl

class:
    patterns:
        a number box
    members:
        payload as integer

implicitly convert source:
    replacement:
        @intrinsic("construct", a number box, source)

function unwrap {a number box:wrapped}:
    execute:
        return the payload of wrapped

print unwrap 17 as line
