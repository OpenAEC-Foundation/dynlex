import lib/std.dl

class:
    patterns:
        a number box
    members:
        payload as integer

implicitly convert source:
    replacement:
        @intrinsic("construct", a number box, source)

function the payload unwrapped from {a number box:wrapped}:
    execute:
        return the payload of wrapped

print (the payload unwrapped from 17) as line
