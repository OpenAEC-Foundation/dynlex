import lib/std.dl

function inspect before shadow for {fixed integer:item count}:
    execute:
        if true:
            set {an array of item count items:values} to [1, 2]
            print the length of values as a line
            set {item count} to 1

inspect before shadow for 2
