import lib/std.dl

class:
    patterns:
        [an|] invalid managed value
    members:
        number as an integer
    retain:
        return 1
    release:
        ignore @intrinsic("lifecycle value")

set value to @intrinsic("construct", an invalid managed value, 1)
ignore value
