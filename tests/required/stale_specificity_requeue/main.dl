function the shutdown code for item and user:
    replacement:
        @intrinsic("add", item, user)

function the lights shutdown code:
    replacement:
        the shutdown code for lights and 4

function the shutdown code for lights and user:
    replacement:
        the shutdown code for placeholder and user
