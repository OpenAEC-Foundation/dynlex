import lib/std.dl

set storage to the checked allocation from (0 as a pointer) resized to 0 items of (a 64 bit floating-point number array of 0 items)
@intrinsic("call", "libc", "free", nothing, storage)
