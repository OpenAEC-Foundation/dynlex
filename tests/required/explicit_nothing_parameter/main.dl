function nothing:
    replacement:
        @intrinsic("type", "nothing")

function finish:
    execute:
        @intrinsic("return")

flex function continue after {nothing:action}:
    replacement:
        @intrinsic("discard", action)

continue after finish
