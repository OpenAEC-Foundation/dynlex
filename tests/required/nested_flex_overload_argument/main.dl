import lib/std.dl

function consume value:
    execute:
        ignore value

function demo:
    execute:
        set text to a new string from "a" containing 1 byte
        consume the character at 0 in text

demo
