function set variable to value:
	replacement:
		@intrinsic("store", variable, value)

function ignore value:
	replacement:
		@intrinsic("discard", value)

function [an|] integer:
	replacement:
		@intrinsic("type", "int")

function fixed quantity:
	replacement:
		@intrinsic("fix", quantity)

function say message:
	execute:
		ignore @intrinsic("variadic call", "libc", "printf", an integer, 1, "%s\n", message)

function value as [a|] {fixed integer:bits} bit integer:
	replacement:
		@intrinsic("cast", value, @intrinsic("type", "int", bits))

function print message:
	execute:
		ignore @intrinsic("variadic call", "libc", "printf", an integer, 1, "%ld\n", message as a 64 bit integer)

function run the custom adder:
	execute:
		say "custom adder:"
		set result to @intrinsic("add", 1, 2)
		print result

run the custom adder
