import lib/std.dl

function value after guaranteed loop:
    execute:
        set keep_going to true
        set result to 0
        loop while keep_going:
            set result to 1
            set keep_going to false
        if result = 1:
            return 1
        else:
            return "wrong"

print value after guaranteed loop as line
