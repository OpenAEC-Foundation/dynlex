import lib/std.dl

class:
    patterns:
        record
    members:
        value as an integer

set container to @intrinsic("construct", record, 1)
set address to the address of the value of container
@intrinsic("store at", address, 5)
print the value of container as a line
