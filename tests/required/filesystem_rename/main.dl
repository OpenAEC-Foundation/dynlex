import lib/filesystem.dl

set source to "tests/required/filesystem_rename/source.tmp"
set destination to "tests/required/filesystem_rename/moved.tmp"
delete the filesystem entry at source
delete the filesystem entry at destination

write the string form of "contents" to the file at source
rename the filesystem entry at source to destination
print whether it succeeded as a line
print whether source is readable as a line
print whether destination is readable as a line

delete the filesystem entry at destination
