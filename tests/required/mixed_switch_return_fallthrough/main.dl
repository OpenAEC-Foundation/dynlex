import lib/std.dl

function the value chosen for choice:
    execute:
        match on choice:
            case 1:
                return 10
            otherwise:
                ignore 20

print the value chosen for 1 as a line
