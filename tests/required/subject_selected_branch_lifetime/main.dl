import lib/filesystem.dl

function read and print {fixed integer:choice}:
    execute:
        if choice = 1:
            read the file at "/tmp/dynlex-subject-branch.txt"
        print (the contents of it) as a line

write the string form of "hello" to the file at "/tmp/dynlex-subject-branch.txt"
read and print 1
delete the filesystem entry at "/tmp/dynlex-subject-branch.txt"
