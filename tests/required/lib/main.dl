#this file tests the standard libraries.

import vector.dl

set sample to a new vector with x 0.0, y 1.0 and z 2.0
add 3 to sample
multiply sample by 4.5

#classes should have a print function, which prints their name (first syntax) and members. classes can override it with a print: section
print sample

function test:
	execute:

test
