flex function [an|] integer:
    replacement:
        @intrinsic("type", "int")

class:
    patterns:
        [a|] box
    members:
        value as an integer

@intrinsic("return", @intrinsic("construct", a box, 1))
