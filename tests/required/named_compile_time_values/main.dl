import lib/graphics.dl

function the selected value for {fixed integer:value}:
    execute:
        return value

print the selected value for the escape key as a line
print the selected value for the A key as a line
