import lib/std.dl

function compile-time selected value for {fixed integer:n}:
    execute:
        if n = 2:
            return 11
        else:
            return "unreachable"

function immediately returned value:
    execute:
        return 12
        return "unreachable"

print compile-time selected value for 2 as line
print immediately returned value as line
