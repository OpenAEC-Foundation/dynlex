import lib/std.dl

function the compile-time selected value for {fixed integer:choice}:
    execute:
        if choice = 2:
            return 11
        else:
            return "unreachable"

function immediately returned value:
    execute:
        return 12
        return "unreachable"

print the compile-time selected value for 2 as a line
print immediately returned value as a line
