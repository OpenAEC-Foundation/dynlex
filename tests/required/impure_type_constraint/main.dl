function integer:
    replacement:
        @intrinsic("type", "int")

function bad type:
    replacement:
        @intrinsic("discard", @intrinsic("call", "libc", "puts", integer, "side effect"))
        integer

function accept {bad type:value}:
    execute:
        @intrinsic("discard", value)
