#this test tests circular imports
import other.dl
import std.dl

function print the imported message:
	execute:
		print "printing from main.dl"

# We start a relay through the imported file before printing the message.
function start the imported message relay:
	execute:
		relay the imported message

start the imported message relay
