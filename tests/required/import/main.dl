#this test tests circular imports
import other.dl
import std.dl

function first:
	execute:
		print "printing from main.dl"

# we call third, which calls second, which calls first, which prints the message.
function third:
	execute:
		second

third