import lib/std.dl

flex function finish with [integer {integer:value}|floating {floating-point number:value}] and {fixed integer:remaining}:
    replacement:
        if remaining > 0:
            finish with floating 1.5 and (remaining - 1)
        else:
            print 1 as a line

finish with integer 1 and 1
