import lib/std.dl

function emit {argument list:arguments}:
    execute:
        print arguments' length as a line
        loop index from 0 to arguments' length:
            set value to the argument at index in arguments
            print value as a line
            print value's length as a line

set arguments to the command line arguments
emit arguments
print "end" as a line
