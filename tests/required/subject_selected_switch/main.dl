import lib/std.dl

function publish choice for {fixed integer:choice}:
    execute:
        match on choice:
            case 1:
                set the subject to 10
            otherwise:
                set the subject to 20
        print it as a line

publish choice for 1
publish choice for 2
