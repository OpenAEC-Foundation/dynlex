import lib/filesystem.dl

set path to the string form of "tests/required/filesystem_paths/string.tmp"
delete the filesystem entry at path

write the string form of "string path" to the file at path and print whether it succeeded as a line
delete the filesystem entry at path
print whether it succeeded as a line
