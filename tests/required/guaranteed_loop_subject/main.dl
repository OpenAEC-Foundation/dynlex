import lib/std.dl

function publish subject in guaranteed loop:
    execute:
        set ongoing to true
        loop while ongoing:
            set the subject to 1
            set ongoing to false
        print it as a line

publish subject in guaranteed loop
