import lib/std.dl

function the value for condition:
    execute:
        if condition:
            return 1
        loop while true:
            ignore 0
        return 2

print the value for @intrinsic("equal", @intrinsic("call", "libc", "abs", an integer, -1), 1) as a line
