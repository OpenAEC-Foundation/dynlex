import lib/std.dl

function run static branch:
    execute:
        if true:
            ignore 1
        else:
            set unreachable to 2
            print unreachable as a line

run static branch
