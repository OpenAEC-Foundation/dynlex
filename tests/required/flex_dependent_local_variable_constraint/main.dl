import lib/std.dl

flex function the flex local length for {fixed integer:item count}:
    replacement:
        set {an array of item count items:values} to [1, 2]
        the length of values

print the flex local length for 2 as a line
