import lib/std.dl

class:
    patterns:
        [a|] duplicate managed value
    members:
        number as an integer
    retain:
        ignore @intrinsic("lifecycle value")
    retain:
        ignore @intrinsic("lifecycle value")
    release:
        ignore @intrinsic("lifecycle value")
