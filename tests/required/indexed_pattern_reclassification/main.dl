function argument called:
    replacement:
        @intrinsic("return", argument)

function value used with sentinel:
    replacement:
        @intrinsic("discard", value)
        hold sentinel

function argument called:
    replacement:
        hold argument

function [target|objective] for first and second:
    replacement:
        first called
        second used with sentinel
