import lib/std.dl

function the selected value for {fixed integer:choice}:
    execute:
        match based on choice:
            case choice:
                return 17
            otherwise:
                return 0

print the selected value for 3 as a line
