import lib/std.dl

class:
    patterns:
        a number box
    members:
        payload as integer

convert {integer:input}:
    replacement:
        @intrinsic("construct", a number box, input)

set input to 23
set wrapped to input as a number box
print the payload of wrapped as line
print input as line
