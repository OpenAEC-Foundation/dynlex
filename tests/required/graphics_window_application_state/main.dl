import lib/graphics.dl

exposed function exercise the application state API for {pointer:window}:
    execute:
        set {application pointer} to 0 as a pointer to a byte
        set the application state of window to application pointer
        set retrieved to the application state of window
        ignore retrieved

print "window application state is readable" as a line
return 0
