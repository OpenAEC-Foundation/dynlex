function value used with sentinel:
    replacement:
        @intrinsic("discard", value)
        hold sentinel

function the recurrence of argument with marker:
    replacement:
        @intrinsic("discard", argument)
        the recurrence of marker with marker
        missing used with sentinel
