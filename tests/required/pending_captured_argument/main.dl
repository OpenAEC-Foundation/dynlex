import lib/std.dl

function a string buffer measuring length:
    execute:
        allocate length bytes
        set address to it
        return a new string from address containing length bytes

ignore a string buffer measuring 1
