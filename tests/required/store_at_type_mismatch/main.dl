import lib/std.dl

class:
    patterns:
        [a|] box
    members:
        value as an integer

set target to 1
set instance to @intrinsic("construct", a box, 2)
@intrinsic("store at", @intrinsic("address of", target), instance)
