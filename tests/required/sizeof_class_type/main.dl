import lib/std.dl

class:
    patterns:
        [a|] pair value
    members:
        left as a 32 bit integer
        right as a 32 bit integer

print the size of a 32 bit integer as a line
print the size of a pair value as a line
