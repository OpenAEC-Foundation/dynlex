# Closest points and containment

The implementation, ownership contract and combined Rust comparison are
documented in [the crossing verification](../cross2/README.md).
`main.dl` translates all 16 tests from the pinned `geom2d/containment.rs`.
