import lib/shader.dl

# Passthrough vertex shader — copies input position to gl_Position
# Compile with: ./build/dynlex games/passthrough_vertex.dl --emit-spirv --shader-stage=vertex -o games/passthrough.spv

set the output position to the vertex x the vertex y the vertex z the vertex w
